export enum Role {
  owner = 'owner',
  instructor = 'instructor',
  student = 'student',
  platform_admin = 'platform_admin',
}

export enum BeltLevel {
  white = 'white',
  blue = 'blue',
  purple = 'purple',
  brown = 'brown',
  black = 'black',
}

export enum TechniquePosition {
  guard = 'guard',
  half_guard = 'half_guard',
  mount = 'mount',
  back = 'back',
  side_control = 'side_control',
  turtle = 'turtle',
  standing = 'standing',
  other = 'other',
}

export enum TechniqueType {
  submission = 'submission',
  sweep = 'sweep',
  escape = 'escape',
  transition = 'transition',
  takedown = 'takedown',
  other = 'other',
}

export enum SubscriptionTier {
  starter = 'starter',
  growth = 'growth',
  pro = 'pro',
  franchise = 'franchise',
}
